#include "fila.h"
#include <stdio.h>
#include <stdlib.h>

int main(void)
{

    enfileirar(1);
    enfileirar(2);
    enfileirar(3);
    enfileirar(3);
    enfileirar(3);

    exibir();

    return 0;
}